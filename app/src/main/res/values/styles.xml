<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:statusBarColor">#d561c9</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">30sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="CustomMaterialDialog" parent="Theme.AppCompat.Dialog.Alert">
        <!-- Background Color-->
<!--        <item name="android:background">#ffffff</item>-->
        <!-- Text Color for title and message -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <!-- Text Color for buttons -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        ....
    </style>

    <style name="TimePickerDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#32acba</item>
    </style>

</resources>
